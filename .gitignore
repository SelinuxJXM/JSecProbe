# Dependencies
node_modules
.pnpm-store

# Build outputs
dist
dist-electron
dist-renderer
dist_tmp
dist_final
release
release_app

# Local data
dev-data
*.db
*.sqlite
*.log

# Environment files
.env
.env.local
.env.*.local

# IDE / Editor
.vscode
.idea
.trae/

# WorkBuddy 本地工作数据（会话记忆/备份，不推送）
.workbuddy/

# 旧仓库 .git 备份（pack 损坏，已被全新克隆替换，确认无需要后可删除）
.git.broken-backup/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS files
.DS_Store
Thumbs.db

# Temp files
*.tmp
*.temp
*.bak

# Sensitive files (contain secrets/tokens)
upload.ps1
upload-release.ps1
scripts/upload-release.js
passwd.md
项目提示词.txt

# Local data directory
JSecProbeData

# Screenshots 由 README 通过 raw 链接引用，全部纳入版本跟踪（勿忽略）

# Tesseract traineddata (OCR 语言包由 tesseract.js 运行时自动下载)
*.traineddata
# 但随包发布的离线语言包必须入库，否则打包后 OCR 无语言数据可用
!resources/tessdata/*.traineddata

# 临时调试脚本（根目录下 _ 开头的一次性检查脚本）
_*.js
_*.ts
_*.ps1

# 临时备份文件
original_index.vue

# 构建 / 缓存产物（不提交）
*.tsbuildinfo
dist-output/
.tools/
